apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: flink-gateway
  namespace: flink-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - hosts:
    - "*"
    port:
      number: 80
      name: http
      protocol: HTTP
    tls:
      httpsRedirect: false # TODO
  # TODO
  # - hosts:
  #   - "*"
  #   port:
  #     number: 443
  #     name: https
  #     protocol: HTTPS
  #   tls:
  #     mode: SIMPLE
  #     credentialName: TODO
